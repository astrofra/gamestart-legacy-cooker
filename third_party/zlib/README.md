# zlib

This directory vendors the zlib C sources needed by the standalone native
`gamestart-cooker-c` tool.

The files were copied from the legacy GameStart source tree under
`source/extern/zlib`. The upstream zlib license is included in `zlib.h`.

